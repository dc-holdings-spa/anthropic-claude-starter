Revisa el diff actual usando el skill `pr-review` o delega al subagent `reviewer`.

Si $ARGUMENTS contiene path específico, focalizar el review en ese archivo/directorio.
Si no, revisar `git diff main...HEAD` completo.

Output formato estructurado obligatorio.